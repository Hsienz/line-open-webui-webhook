[project]
name = "line-open-webui-webhook"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiofiles>=25.1.0",
    "dotenv>=0.9.9",
    "flask>=3.1.3",
    "line-bot-sdk>=3.22.0",
]
